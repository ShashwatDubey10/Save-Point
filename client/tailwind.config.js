/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'selector', // Enable class-based dark mode (looks for .dark class)
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
}
